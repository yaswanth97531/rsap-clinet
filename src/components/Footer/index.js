import React from 'react';
import { Layout, Icon } from 'antd';

const { Footer } = Layout;

const FooterNav = () => {
  return(
    <Layout>
    <Footer style={{ color: 'red', textAlign: 'center' }}>Footer</Footer>
  </Layout>
  );
  
}

export default FooterNav;